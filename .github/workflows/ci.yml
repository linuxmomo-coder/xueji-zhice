name: CI (temporarily paused for atomic v0.3.0 payload)

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v0.3.0 remediation payload is being applied atomically"
