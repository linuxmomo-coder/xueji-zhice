name: CI (remediation in progress)

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Source remediation is being applied; full CI will be restored in the final commit."
